Created using...

`npm init -y`

`npm install jest`

...And then editing `package.json` to change the `test` block to...

```json
  "scripts": {
    "test": "jest"
  },
  ```

# Using the Project

Run tests using `npm test`